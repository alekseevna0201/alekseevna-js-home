let tasksCompleted = {
  'Anna': 29,
  'Serg': 35,
  'Elena': 1,
  'Anton': 99
};


function getBestWorker() {
  for (var i = 0; i < tasksCompleted.length; i++) {
    let bestWorker = {};
    if (bestWorker in tasksCompleted) {
      continue; // если да - всё, берём следующее
    }

     // если нет - запоминаем, что это значение уже встречалось
    console.log(Value); // выводим его в консоль
  }
}
