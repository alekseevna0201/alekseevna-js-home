let getBest = require('./task1');

describe('Get best employer', () => {
  it('get best employer who has max value issues', () => {
    let tasksCompleted;
    let getBest = getBest(tasksCompleted[value]);
    expect(getBest).toEqual('Anton');
  });
});