const sum = require('task2');

describe('Hash', () => {
  it('hash', () => {
    let image = {
      width: 100,
      height: 400,
      title: 'Cool image'
    };
    expect( ).toBe['100', '110', '200'];
  });
})